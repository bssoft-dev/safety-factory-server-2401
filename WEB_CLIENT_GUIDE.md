# 🌐 Safety Server 웹 클라이언트 사용 가이드

## 📍 접속 방법

Safety Server가 실행 중일 때, 웹 브라우저에서 다음 URL로 접속하세요:

```
http://localhost:24015/webclient
```

**원격 접속 시:**
```
http://[서버IP]:24015/webclient
```

## 🎯 주요 기능

### ✅ 실시간 음성 통화
- **마이크 입력**: 브라우저에서 마이크 접근 권한 허용 필요
- **스피커 출력**: 다른 참가자들의 음성을 실시간으로 수신
- **양방향 통신**: 동시 송수신 지원

### ⚙️ 설정 옵션
- **서버 주소**: 자동으로 현재 호스트 감지 (수동 변경 가능)
- **포트**: 24015 (기본값)
- **방 이름**: 참가할 방 이름 설정
- **디바이스 ID**: 고유 식별자
- **샘플레이트**: 16000Hz (권장) / 48000Hz
- **볼륨 조절**: 0-100% 슬라이더
- **음소거**: 체크박스로 출력 음소거

### 📊 실시간 모니터링
- **송신/수신 프레임 카운터**
- **연결 시간 표시**
- **지연시간 측정** (예정)
- **실시간 로그 출력**

## 🚀 사용 단계

### 1단계: 방 생성
```
🏠 방 생성 버튼 클릭
→ 서버에 새로운 방 생성 요청
→ 성공 시 로그에 확인 메시지 표시
```

### 2단계: 연결
```
🔗 연결 버튼 클릭
→ 마이크 권한 요청 (허용 필요)
→ WebSocket 연결 수립
→ 상태가 "연결됨"으로 변경
```

### 3단계: 음성 통화
```
✅ 연결 완료 후 자동으로 음성 송수신 시작
→ 마이크로 말하기
→ 다른 참가자 음성 듣기
→ 실시간 통계 확인
```

### 4단계: 연결 해제
```
❌ 연결 해제 버튼 클릭
→ WebSocket 연결 종료
→ 오디오 스트림 정리
→ 상태가 "연결 안됨"으로 변경
```

## 🔧 브라우저 설정

### 마이크 권한 허용
```
Chrome: 🔒 주소창 왼쪽 아이콘 클릭 → 마이크 허용
Firefox: 🔒 주소창 왼쪽 자물쇠 → 권한 → 마이크 허용
Safari: Safari → 환경설정 → 웹사이트 → 마이크
Edge: ⚙️ 설정 → 사이트 권한 → 마이크
```

### HTTPS 환경 (권장)
```
- 마이크 접근을 위해 HTTPS 환경 권장
- 로컬 테스트: http://localhost는 허용됨
- 원격 접속: HTTPS 설정 필요할 수 있음
```

## 🛠️ 문제 해결

### 🔇 마이크가 작동하지 않을 때
1. 브라우저 마이크 권한 확인
2. 시스템 마이크 설정 확인
3. 다른 앱에서 마이크 사용 중인지 확인
4. 브라우저 새로고침 후 재시도

### 🔌 연결이 안 될 때
1. Safety Server가 실행 중인지 확인
2. 방이 미리 생성되었는지 확인
3. 포트 24015가 열려있는지 확인
4. 방화벽 설정 확인

### 🔊 소리가 안 들릴 때
1. 볼륨 설정 확인 (0-100%)
2. 음소거 체크박스 해제
3. 시스템 스피커 볼륨 확인
4. 다른 참가자가 있는지 확인

### 📶 연결이 불안정할 때
1. 네트워크 연결 상태 확인
2. 브라우저 콘솔에서 오류 메시지 확인
3. 서버 로그 확인
4. 재연결 시도

## 🎯 최적 사용 환경

### 권장 브라우저
- ✅ Chrome 90+ (최고 호환성)
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

### 권장 설정
```
샘플레이트: 16000Hz (지연 최소화)
볼륨: 50-70% (피드백 방지)
네트워크: 유선 LAN (안정성)
환경: 조용한 공간 (음질 향상)
```

## 📊 기술 정보

### WebSocket URL 형식
```
ws://[host]:[port]/ws/room/[room_name]/[sample_rate]/float32/[device_id]
```

### 오디오 스펙
```
샘플레이트: 16000Hz / 48000Hz
채널: 모노 (1채널)
데이터 타입: Float32 (웹) / Int16 (Python)
프레임 크기: 1024 샘플 (64ms @ 16kHz)
```

### API 엔드포인트
```
GET  /webclient           - 웹 클라이언트 페이지
POST /v1/create_room      - 방 생성
GET  /v1/rooms           - 방 목록 조회
WS   /ws/room/...        - 오디오 스트리밍
```

## 🚀 개발자 정보

소스코드 위치:
- **웹 클라이언트**: `/static/index.html`
- **서버 설정**: `app.py`
- **음성 처리**: `services/voice_chat.py`

커스터마이징 가능 요소:
- UI 디자인 변경
- 오디오 필터 추가
- 통계 데이터 확장
- 보안 기능 강화

---
**🎵 Safety Server Web Client - BS Soft**
