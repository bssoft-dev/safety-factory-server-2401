# �� Safety Server 단순 WebSocket 스트리밍 테스트

## 🌐 접속 방법

**웹 브라우저에서 다음 URL로 접속:**

### HTTP 접속 (권장)
```
http://safety-server.bs-soft.co.kr:24015/webclient
```

### HTTPS 접속 (SSL 설정된 경우)
```
https://safety-server.bs-soft.co.kr/webclient
```

### 로컬 테스트
```
http://localhost:24015/webclient
```

## 🎯 페이지 기능

### ✅ 간소화된 기능
- **WebSocket 실시간 오디오 송수신**만 테스트
- 방 생성 기능 제거 (서버에서 미리 생성)
- 복잡한 설정 제거
- 깔끔한 UI로 개선

### 🔧 설정 항목
- **서버 주소**: `safety-server.bs-soft.co.kr` (자동 설정)
- **포트**: `24015`
- **방 이름**: `테스트방` (기본값)
- **디바이스 ID**: `webclient-001`
- **샘플레이트**: `16000 Hz` (권장)
- **프로토콜**: 자동 감지 또는 수동 선택

### 📊 실시간 모니터링
- **송신 프레임**: 마이크에서 서버로 전송된 프레임 수
- **수신 프레임**: 서버에서 받은 오디오 프레임 수
- **연결 시간**: WebSocket 연결 지속 시간

## 🚀 사용법

### 1단계: 연결하기
```
1. 웹 페이지 접속
2. 설정 확인 (기본값 사용 권장)
3. 🔗 연결 버튼 클릭
4. 마이크 권한 허용
5. ✅ "연결됨 - 스트리밍 중" 상태 확인
```

### 2단계: 오디오 테스트
```
1. 마이크로 말하기 → 송신 프레임 증가 확인
2. 다른 클라이언트 연결 시 → 수신 프레임 증가 확인
3. 볼륨 조절 (0-100%)
4. 음소거 체크박스로 출력 끄기/켜기
```

### 3단계: 연결 해제
```
1. ❌ 연결 해제 버튼 클릭
2. "연결 해제됨" 상태 확인
3. 오디오 스트림 자동 정리
```

## 🔧 프로토콜 설정

### HTTP 환경
```
페이지: http://safety-server.bs-soft.co.kr:24015/webclient
프로토콜: 자동 감지 (WS 사용)
상태: ✅ 정상 작동
```

### HTTPS 환경
```
페이지: https://safety-server.bs-soft.co.kr/webclient
프로토콜: WSS (보안) 선택 필요
상태: ⚠️ 서버 SSL 설정 필요
```

## 📱 다중 클라이언트 테스트

### 동일한 방에 여러 클라이언트 접속
```
1. 브라우저 탭 1: http://safety-server.bs-soft.co.kr:24015/webclient
   - 방 이름: "테스트방"
   - 디바이스 ID: "client-001"

2. 브라우저 탭 2: http://safety-server.bs-soft.co.kr:24015/webclient  
   - 방 이름: "테스트방"
   - 디바이스 ID: "client-002"

3. 각각 연결하여 실시간 음성 통화 테스트
```

## 🎵 오디오 설정

### 권장 설정
```
샘플레이트: 16000 Hz (지연 최소화)
볼륨: 50% (피드백 방지)
음소거: 필요시 체크
```

### 브라우저별 마이크 권한
```
Chrome: 주소창 🔒 → 마이크 허용
Firefox: 주소창 🔒 → 권한 → 마이크
Safari: Safari → 환경설정 → 웹사이트 → 마이크
Edge: 설정 → 사이트 권한 → 마이크
```

## 🔍 문제 해결

### 연결 안됨
```
1. 서버 실행 상태 확인: netstat -tlnp | grep 24015
2. 방 생성 확인: curl -X POST http://localhost:24015/v1/create_room -d '{"room_name":"테스트방"}'
3. 프로토콜 설정 확인 (WS/WSS)
```

### 마이크 오류
```
1. 브라우저 마이크 권한 허용
2. 시스템 마이크 설정 확인
3. 다른 앱에서 마이크 사용 중인지 확인
```

### 소리 안들림
```
1. 볼륨 설정 확인 (0-100%)
2. 음소거 체크박스 해제
3. 시스템 스피커 설정 확인
4. 다른 클라이언트가 접속했는지 확인
```

## 📊 로그 메시지 의미

### 성공적인 연결
```
[시간] 🌐 페이지 로드 완료 - 연결 버튼을 클릭하여 시작하세요
[시간] ✅ 오디오 설정 완료
[시간] 🔗 연결 시도: ws://safety-server.bs-soft.co.kr:24015/ws/room/테스트방/16000/float32/webclient-001
[시간] ✅ WebSocket 연결 성공 - 오디오 스트리밍 시작
```

### 일반적인 오류
```
❌ 오디오 설정 실패: → 마이크 권한 확인
❌ WebSocket 오류: → 서버 연결 확인
💡 HTTPS 환경: WSS 프로토콜 사용 확인 필요
```

## 🔧 서버 측 방 생성

WebSocket 연결 전에 방이 미리 생성되어야 합니다:

```bash
# 방 생성 API 호출
curl -X POST http://localhost:24015/v1/create_room \
  -H "Content-Type: application/json" \
  -d '{"room_name":"테스트방"}'

# 응답 예시
{"message":"Room '테스트방' created successfully"}
```

## 🎯 테스트 시나리오

### 기본 스트리밍 테스트
```
1. 웹 클라이언트 접속
2. "테스트방" 연결
3. 마이크로 말하기
4. 송신 프레임 카운터 증가 확인
5. 로그에서 연결 상태 확인
```

### 다중 클라이언트 테스트
```
1. 2개 브라우저 탭에서 동시 접속
2. 각각 다른 디바이스 ID 사용
3. 한쪽에서 말하기 → 다른쪽에서 듣기
4. 양방향 통화 테스트
5. 송신/수신 프레임 카운터 확인
```

---
**💡 요약**: 단순화된 웹 클라이언트로 WebSocket 오디오 스트리밍을 쉽게 테스트할 수 있습니다.
