# 라즈베리파이 LiveKit 설정 파일 예제
# config.txt 파일로 저장하여 사용

# ===== 기존 설정 (유지) =====
DEVICE_ID = "0"
IN_DEVICE_NUM = 0
OUT_DEVICE_NUM = 0
CHUNK = 1024
RATE = 16000
WARN_DISTANCE = 3
TARGET_BEACONS = ["yo:ur:ma:ca:dr:es"]

# ===== LiveKit 설정 (추가) =====
# 통신 모드 선택: "websocket" 또는 "livekit"
communication_mode = "livekit"

# LiveKit 서버 설정
room_name = "safety-room"
livekit_url = "wss://livekit.bs-soft.co.kr"
api_base_url = "https://safety-server.bs-soft.co.kr:24015"

# 참가자 정보
participant_name = "RaspberryPi-001"
participant_identity = "raspberry-001"

# ===== 기존 WebSocket 설정 (비활성화) =====
# ws_host = 'wss://localhost:24015/ws/room'
# host = 'https://localhost:24015'

# ===== 네트워크 설정 예제 =====
# 로컬 네트워크 사용 시:
# livekit_url = "ws://192.168.1.100:7880"
# api_base_url = "http://192.168.1.100:24015"

# 실제 운영 서버 (HTTPS/WSS 사용):
# livekit_url = "wss://livekit.bs-soft.co.kr"
# api_base_url = "https://safety-server.bs-soft.co.kr:24015"

# ===== 오디오 최적화 설정 =====
# 낮은 지연시간 (더 많은 CPU 사용):
# CHUNK = 512
# RATE = 16000

# 높은 안정성 (더 많은 지연시간):
# CHUNK = 2048
# RATE = 16000

# ===== 디바이스별 설정 예제 =====
# 작업반장용 디바이스:
# participant_name = "Supervisor-001"
# participant_identity = "supervisor-001"

# 작업자용 디바이스:
# participant_name = "Worker-001"
# participant_identity = "worker-001"
